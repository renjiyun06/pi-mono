/**
 * AgentSession pool — manages one session per QQ user_id.
 *
 * Uses createAgentSession() from pi-coding-agent SDK.
 * Sessions share the same cwd (workspace) but have
 * isolated conversation histories.
 */

// TODO: implement
